apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "karve.fullname" . }}-config
  labels:
    {{- include "karve.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
