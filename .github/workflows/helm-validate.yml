name: Helm Chart Validation

on:
  pull_request:
    paths:
      - "charts/veneer/**"
  push:
    branches:
      - main
    paths:
      - "charts/veneer/**"

jobs:
  validate:
    name: Validate Helm Chart
    runs-on: runs-on=${{ github.run_id }}/runner=2cpu-linux-x64/cache=/var/cache/magic/extras=magic-cache

    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Helm
        uses: azure/setup-helm@1a275c3b69536ee54be43f2070a358922e12c8d4 # v4
        with:
          version: v3.16.3

      - name: Helm Lint
        run: |
          helm lint charts/veneer

      - name: Build chart dependencies
        run: |
          helm dependency update charts/veneer

      - name: Create Kind cluster
        uses: helm/kind-action@a1b0e391336a6ee6713a0583f8c6240d70863de3 # v1
        with:
          version: v0.26.0
          kubectl_version: v1.31.0

      - name: Install chart
        run: |
          helm install veneer-test charts/veneer \
            --namespace veneer-system \
            --create-namespace \
            --values charts/veneer/ci/ci-values.yaml \
            --wait \
            --timeout 5m

      - name: Verify deployment
        run: |
          kubectl get pods -n veneer-system
          kubectl get deployment -n veneer-system
          kubectl rollout status deployment/veneer-test-controller-manager -n veneer-system --timeout=5m

      - name: Check service endpoints
        run: |
          kubectl get svc -n veneer-system
          kubectl get endpoints -n veneer-system

      - name: Get pod logs
        if: failure()
        run: |
          kubectl logs -n veneer-system -l app.kubernetes.io/name=veneer --tail=100

      - name: Uninstall chart
        if: always()
        run: |
          helm uninstall veneer-test -n veneer-system || true
